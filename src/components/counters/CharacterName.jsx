import styles from './CharacterName.module.scss';
import Health from './Health';

const CharacterName = props => {
    return <p className={`${styles['character-name']}`}>TOGA</p>   
}

export default CharacterName;